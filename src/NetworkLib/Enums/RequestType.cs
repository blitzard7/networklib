namespace NetworkLib.Enums
{
    /// <summary>
    /// Represents the RequestType enumeration.
    /// </summary>
    public enum RequestType
    {
        Connect,
        SendData,
        IsAlive,
        Disconnect
    }
}